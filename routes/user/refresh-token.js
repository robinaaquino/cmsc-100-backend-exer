/**
 * Refresh Token Module
 * 
- When called with an expired token, it should not work
- When called with an unexpired token, it should replace the token and return that token in both the session and in payload response
 */